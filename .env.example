APP_ENV=development
APP_URL=http://localhost:8000
DB_HOST=localhost
DB_NAME=iasdsni
DB_USER=root
DB_PASS=
JWT_SECRET=change-me

# Absolute path to the frontend document root maintenance flag.
# Apache reads this file from the deployed public web root before React loads.
# Example cPanel/FTP: /home/user/public_html/maintenance.flag
MAINTENANCE_FLAG_PATH=
